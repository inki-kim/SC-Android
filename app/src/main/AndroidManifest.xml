<?xml version="1.0" encoding="utf-8"?><!--
  ~ Dongyang Mirae University
  ~ Project 'ServersCenter'
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.semicolon.serverscenter">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_appicon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/TransparentStatusBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.signIn.SignInActivity"
            android:theme="@style/TransparentStatusBarTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".view.signUp.SignUpActivity"
            android:theme="@style/TransparentStatusBarTheme"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name=".view.search.PasswordResetActivity"
            android:theme="@style/TransparentStatusBarTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".view.main.MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".view.chatbot.ChatbotActivity" />
        <activity android:name=".view.chat.MessageActivity"></activity>
    </application>

</manifest>